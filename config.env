# Cost Guard Configuration
# ========================

# Your VM's instance ID (REQUIRED)
INSTANCE_ID="$(hostname)"

# CPU threshold (%) - VM is "idle" below this (based on 5-min load avg)
# TODO: Add test/production mode switching
CPU_THRESHOLD=100

# Idle duration (seconds) before shutdown - 1800 = 30 minutes
IDLE_THRESHOLD_SECONDS=60

# Minimum uptime (seconds) before allowing shutdown
MIN_UPTIME_SECONDS=60

# Log file
LOG_FILE="/var/log/cost-guard.log"

# State file (tracks when idle started)
STATE_FILE="/var/run/cost-guard/idle_since"
